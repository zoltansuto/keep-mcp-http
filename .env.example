# Google Keep MCP Server Configuration

# Server configuration
MCP_HOST=127.0.0.1
MCP_PORT=8000

# Google Keep credentials (use get_master_token.py to obtain the master token)
GOOGLE_EMAIL=your_google_email@gmail.com
GOOGLE_MASTER_TOKEN=your_google_master_token

# Safety settings (set to true to allow modification of notes without keep-mcp label)
UNSAFE_MODE=false

# REST API configuration
REST_API_HOST=0.0.0.0
REST_API_PORT=8001